# pi-compose
An over-engineered solution for using `docker-compose` on a remote server.

_Because creating a docker-container on a remote machine shouldn't be so complicated._

## Installation
```bash
git clone https://github.com/TheSpeedM/pi-compose
cd pi-compose
python3 setup.py install
```
